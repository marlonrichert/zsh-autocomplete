#!/bin/zsh
emulate -L zsh -o extendedglob

local func; for func in $@; do
  [[ -v functions[$func] ]] &&
    unfunction $func
  builtin autoload +X -Uz $func
  functions[.autocomplete.$func]=$functions[$func]
done
